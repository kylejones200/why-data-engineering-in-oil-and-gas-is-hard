[project]
name = "why-data-engineering-in-oil-and-gas-is-hard"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.10.0",
    "pytest>=8.0",
    "spacy>=3.8.14",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
